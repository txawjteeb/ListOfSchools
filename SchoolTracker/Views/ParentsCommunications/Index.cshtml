@model IEnumerable<SchoolTracker.Models.ParentsCommunication>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClassRoomDojo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PeachJar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EduText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.etc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClassRoomDojo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PeachJar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EduText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.etc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
